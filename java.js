console.log(3=="3")
console.log(3==="3")

// The diffrence is that the lose comparison converts both to the same typeof. The strikt does Not
// We should use the strict one because it is more error prove and more precise.
// It would set the value of the variable

var store = true
console.log(!store ? "hey":"ho")


// it will print out the first value that is true wich is "Stacey"